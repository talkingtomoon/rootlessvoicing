# Rootless Voicing Trainer

재즈 rootless voicing(좌손 4성부) 12키 암기 연습용 SPA. 로컬 전용, 서버/로그인 없음.
목표는 암기와 산출이지 이론 교육이 아니다. 설명 텍스트 최소화, 반복 루프 우선.

## Commands

- `npm run dev` — dev server
- `npm test` — Vitest (engine unit tests)
- `npm run build` — tsc + vite build

## 데이터 모델 (절대 규칙)

12키를 하드코딩하지 않는다. 보이싱은 **루트로부터의 반음 간격 배열**로 정의하고 런타임에 이조한다.
소스는 `src/engine/` — UI에서 음악 계산을 하지 마라.

### 기준 테이블 (A형만 소스, B형은 파생)

| Quality | A형 intervals | A형 도수 |
|---|---|---|
| `m7` | `[3, 7, 10, 14]` | b3 5 b7 9 |
| `dom7` | `[10, 14, 16, 21]` | b7 9 3 13 |
| `maj7` | `[4, 7, 11, 14]` | 3 5 7 9 |
| `m7b5` | `[3, 6, 10, 12]` | b3 b5 b7 1 |
| `dom7b9` | `[10, 13, 16, 19]` | b7 b9 3 5 |

**B형은 A형의 위 두 성부를 한 옥타브 내려 아래에 놓은 것.** 코드에서 A형으로부터 파생시킨다
(`voicings.ts`의 `getVoicing`). B형 테이블을 손으로 따로 적지 마라.

### 진행

- 메이저 ii–V–I: `m7` → `dom7` → `maj7` (루트 오프셋 +2, +7, 0)
- 마이너 ii–V–i: `m7b5` → `dom7b9` → `m7` (동일 오프셋)

마이너 i가 `m7`이라 메이저 ii와 같은 quality인 것은 **의도된 것**이다.

### Item 식별자

- item = `(rootPc, quality, form)` — 고유 120개 (12 × 5 × 2). id 문자열: `"{rootPc}:{quality}:{form}"`
- SRS 카운트는 item 기준 통합. 같은 Dm7 A형이 "C major · ii"로도 "D minor · i"로도 출제된다.
  문맥별로 별개 항목으로 세지 마라.
- **출제는 120개 완전 랜덤.** 진행 단위로 묶어서 내지 마라 — ii 다음 V가 오면 루트가 5도 아래인 걸
  알아서 반쯤 답이 주어진다.

### 모드 — `오늘` + 탭 2개 × 하위 토글 2개 (대칭 구조)

`오늘`(아래 별도 절)이 첫 탭이자 기본 탭이다. 나머지 둘은 대칭 구조를 유지한다.

- **탐색** (사전, 듣기)
  - `진행별` — 키·진행·폼을 고르면 ii–V–I 세 코드. 배치는 `placeProgression`(진행 문맥).
  - `타입별` — quality × form 하나를 12루트에 걸쳐. 배치는 `buildChord`(canonical). 4도권/반음계 순.
- **암기** (테스트, `DrillRunner` 공유)
  - `전체` — 120개에서 N장(12/30/60/120) 랜덤.
  - `타입별` — quality × form 하나의 12루트 한 바퀴 (`itemsOf`).

암기 두 갈래는 `DrillRunner`(보관함 루프 실행부)를 공유한다 — 출제·채점·집계 UI를 복제하지 마라.
어떤 item을 낼지는 `draw: () => Item[]` prop으로만 갈린다.
탭 이름과 하위 토글 이름이 겹치지 않게 하라 (`타입별`은 각 탭 안의 하위 토글 전용).

### 문맥(키·도수) 표시 규칙

- **탐색 모드**: 항상 병기 (스펙 §3). 코드 심볼이 히어로, 키·도수는 그 위 작은 eyebrow.
- **암기 모드**: 문제 화면에는 **코드 심볼 + 폼만**. 문맥은 **정답 공개 후에만** 사후 정보로 표시한다.
  문제에 키·도수를 같이 띄우면 답을 좁혀준다.

### 옥타브 배치 — 두 층

**진행 문맥이 있으면 `placeProgression`, 없으면 canonical** — 이 한 줄이 배치 규칙 전부다.

- **canonical** (`placeVoicing` / `buildChord`): 이조 후 최저음이 MIDI 48–59 (C3–B3).
  암기 모드 정답 표시 + 탐색 타입별처럼 ii–V–I 문맥이 없는 곳.
- **진행 배치** (`placeProgression`): 첫 코드(ii)는 canonical(이게 앵커), 이후 코드는 ±1 옥타브
  보정으로 보이스리딩 유지. **탐색 모드는 개별 코드 클릭·연속 재생 모두 이것만 쓴다** —
  탐색은 항상 진행 문맥 안이므로 canonical을 섞지 마라.
- `placeProgression` 결과는 12키 × {major,minor} × {A,B} = 48진행 전체가
  `progression.snapshot.ts`에 동결돼 있다. 보정 로직을 의도적으로 바꿀 때만
  `npx tsx scripts/gen-progression-snapshot.ts`로 재생성하고 손검산 앵커를 재확인하라.

### 채점 (암기 모드) — 옥타브 관대

`gradeAttempt` (`grading.ts`): 시도한 4음이 정답 보이싱의 **온옥타브 이동형**이고 전체가
**MIDI 41–72 (F2–C5)** 안이면 정답. 다른 옥타브로 맞혔어도 정답 처리하되,
정답 표시는 항상 canonical 하나로 보여준다. 옥타브 단위 이동은 전체가 함께 이동한 것만 인정
(일부 성부만 이동하면 오답).

**화면 건반도 F2–C5 (MIDI 41–72)** — 채점 허용 범위와 정확히 일치시킨다.
placeProgression의 하강 보정(최저 B♭2=46)도 이 범위 안이다. 건반 범위를 바꾸면 채점 범위와 같이 움직일 것.

### 코드 심볼 표기

접미사는 quality 하나로 결정 (`QUALITY_SYMBOL`). **마이너 i도 `m7`로 표기** —
9음을 품지만 메이저 ii와 같은 보이싱이므로 같은 라벨을 쓴다 (사용자 결정, 스펙 검산의 `Cm9` 표기보다 우선).

### 음이름 표기

**루트·키 이름은 pitch class당 하나로 고정** (`ROOT_NAMES`): C C# D Eb E F F# G Ab A Bb B.
진행 토닉 기준 철자 로직은 쓰지 않는다 — 같은 item은 어떤 문맥으로 출제돼도 항상 같은 심볼이다
(C# major의 I도, B major의 ii도 `C#`). 그 결과 한 진행 안에서 ♯/♭이 섞일 수 있고, 이는 의도된 것이다.

보이싱 **구성음**은 루트 기준 도수 철자(`spellInterval`) — ♭3은 3도 글자로 보이게. 단 겹임시표는
같은 방향 홑임시표로 단순화한다(`spellVoicing`의 `simplify`): E♭m7♭5의 ♭5 = B𝄫 → `A`.
내부 표기는 ASCII(`Eb`, `F#`, `b3`), 화면 렌더링 시에만 ♭/♯ 글리프로 변환(`toGlyphs`).

## 오늘 (매일 15분, 폰 우선) — `views/TodayView.tsx`, `session/daily.ts`

최종 목표는 **심볼을 보고 왼손을 바로 치는 것**. 그래서 정답 여부와 함께 **반응 속도**를 본다.

- **코스**(`curriculum`): **한 유닛 = 같은 quality를 세 루트로**. 4도권 세 키가 한 블록(`BLOCKS`, 네 덩어리).
  블록 안 순서는 ii → V → I이라 블록을 끝내면 ii–V–I가 세 개 완성된다. 폼당 quality는 한 번만 가르친다
  (마이너 i(m7)는 메이저 ii에서 이미 배운다 — 메이저를 끄면 마이너 단계가 가르친다). 폼당 20유닛, 전체 40유닛.
  조성별로 묶지 않는 이유: 외울 대상은 (루트, quality, 폼) → 네 음이고 조성은 그 계산에 없다.
  같은 모양을 연달아 세 루트로 옮기는 편이 빨리 손에 붙는다
- **기둥 규칙**(`unitPrereqs`/`isEligible`): 기대는 코드가 **단계 2 이상**일 때만 그 유닛을 들인다.
  기둥은 ① 진행에서 바로 앞 코드 ② 마이너 ii∅·V7♭9의 같은 루트 m7·7 ③ B형의 같은 코드 A형.
  단계 2 = 다른 날에 한 번 이상 맞힘 → "Dm7에서 C만 B로" 힌트가 굳은 코드 위에 선다.
  그래서 첫날은 ii만, 다음 날 V, 그다음 날 I이 들어온다. 복습을 틀리면 단계 1로 내려가 새 것도 멈춘다.
  자격을 갖춘 유닛이 하나도 없으면 순서상 첫 유닛으로 떨어진다 — 막히면 안 된다.
  설정으로 꺼서 풀에 없는 기둥은 따지지 않는다
- **다음 유닛**은 저장하지 않고 "안 배운 item이 있는 첫 유닛" 중 자격을 갖춘 것으로 파생한다
  (진도 링크·암기 모드와 저절로 맞는다)
- **새 유닛 들이기**(`shouldIntroduce`): 오늘 안 본 due ≤ 한 판(8), 단계 1 항목 < 10, 하루 3유닛까지.
  하루 유닛 수는 판을 끝내야 센다(`rootless:unitsToday`)
- **판**(`planRound`, 8장): ① 방금 배운 새 코드 ② 오늘 안 본 due ③ 오늘 흔들린 것(절반까지) ④ 나머지.
  출제 루프는 `session.ts` 그대로. 판마다 바로 진도에 기록한다
- **입력 두 갈래** (`RoundRunner`, DrillRunner와 별개 — 입력·속도 판정이 달라서):
  - `화면 건반`: 한 옥타브 건반에 **아래 성부부터 순서대로** 4번(`gradeSequence`). 직전 음보다 위로 쌓으므로
    페이징이 필요 없다. 순서가 폼을 가르므로 네 음이 맞아도 순서가 틀리면 오답.
    `FAST_MS.tap`(6초)보다 늦으면 맞아도 **느림**
  - `피아노`: 실제 피아노로 치고 정답 보기 → 틀림 / 느림 / 바로
- **느림**: 첫 시도에서만 그 판에 한 번 더 나오고(`answerCurrent(..., slow)`), Leitner 단계는 **유지**
- 정답 공개 뒤에만 **연결 힌트**(`engine/links.ts`, `LinkLines`): 모양(성부 간격·맨 아래 도수),
  진행 연결(ii→V는 48진행 전부 한 음만 반음 내림 — 테스트로 고정), 마이너 짝(m7→m7♭5, 7→7♭9는 2·4번째 성부만),
  B형은 A형의 위 두 음을 아래로. 움직임 표시에서만 E♯·B♯·C♭·F♭을 건반 이름으로 바꾼다
- **시간**: 연습 화면이 보일 때만 1초씩 센다(`rootless:timeLog`, 60일 보관). 학습일은 **새벽 4시**에 넘어간다
- 표시: 오늘 m/15분 막대, 이번 주 월~일 원(채운 만큼), 코스 지도(줄 = 폼 × quality, 칸 = 유닛 세 루트).
  **스트릭 숫자·XP·배지는 없다**
- 아이폰: 홈 화면 앱(manifest, apple-touch-icon — `node scripts/gen-icons.mjs`), safe-area, 배포본만 서비스워커
  (`public/sw.js`: HTML 네트워크 우선, 나머지·피아노 샘플 캐시 우선). **사파리와 홈 화면 앱은 localStorage가 따로다**

## 저장 · 진도 (Leitner)

`localStorage`만. IndexedDB 금지. SM-2 금지. 전부 `state/progress.ts`.

- 저장 형태: `{ session: number, items: { [itemId]: { level: 1..5, lastSeenSession } }, lastDay?: 'YYYY-MM-DD' }`
- 간격은 세션 카운터 기준 1/2/4/8/16 (`LEITNER_INTERVALS`). 달력 날짜로 간격을 재지 않는다
- **세션 = 학습일.** 기록 시 `day`(studyDay)를 넘기면 같은 날의 여러 판·세션은 한 세션 번호로 묶인다
  (`nextSessionNo`). 안 한 날은 세지 않으니 쉬어도 복습이 밀려 쌓이지 않는다.
  중도 나가기는 기록하지 않는다. `day` 없이 부르면 예전처럼 매번 +1 (테스트용)
- 같은 학습일에 두 번째로 나온 항목은 단계를 다시 바꾸지 않는다 — 그날 첫 결과가 기록이다
- 갱신: **첫 시도**에 맞혔으면 단계 +1, 아니면 1로. 보관함에서 나중에 맞힌 건 올리지 않는다
  (`Session.firstTry`만 본다). 미학습은 단계 1에서 시작 → 첫 시도 정답이면 2, 오답이면 1.
  느린 정답(`slow`)은 단계 유지, 오늘 처음 배운 항목(`introduced`)은 결과와 무관하게 단계 1
- 선정 우선순위: ① 간격 지난 항목(단계 낮은 순, 같으면 더 밀린 순) ② 미학습 신규 ③ 단계 낮은 순
- 신규는 한 세션에 `min(6, floor(N/2))`개까지 → **초반에는 N보다 적게 뽑힌다(의도된 램프업)**.
  시작 화면의 크기 버튼에 실제 장수(`→ 6장`)를 같이 표시해서 이걸 숨기지 마라
- 암기 `타입별`은 선정 규칙을 건너뛰고 12개를 직접 지정한다. 진도 갱신은 동일하게 한다

진도 표시는 **12루트 × 5quality 히트맵 두 장(A형/B형)** 이 전부다. 그래프 만들지 마라.
칸 색 농도 = 단계, 미학습은 빈 칸.

### 진도 옮기기 (`state/progressCode.ts`)

계정·서버·클라우드 동기화는 여전히 금지(스펙 §10). 대신 **링크에 진도를 통째로 실어** 옮긴다.

- 123바이트(버전 1 + 세션 2 + item당 1) → base64url 164자, 링크 전체 190자 안팎
- item당 바이트: 상위 3비트 = 단계(0=미학습), 하위 5비트 = **나이**(세션번호 − lastSeenSession).
  절대 세션번호가 아니라 나이를 담아야 받는 기기의 세션 번호와 무관하게 간격이 유지된다.
  나이는 31에서 자르는데 최대 간격이 16이라 `isDue` 판정은 완전히 동일하다
- **item 순서(`allItems()`)가 포맷의 일부다** — 바꾸면 옛 링크가 깨진다 (테스트로 고정해 뒀다)
- 코드가 망가졌으면 `decodeProgress`가 null을 준다. 잘못 붙여넣은 코드로 진도를 날리지 마라
- 적용 전에 **양쪽(이 기기 / 가져올 것)을 보여주고 확인**받는다. 자동 적용 금지
- **기본은 합치기**(`mergeProgress`), 덮어쓰기는 옆 버튼이다. 합치기 규칙: 항목마다 단계는 높은 쪽,
  나이(세션번호 − lastSeenSession)는 **더 밀린 쪽** — 복습이 일찍 돌아오는 쪽으로 기운다.
  세션 번호는 기기마다 다르므로 절대 번호가 아니라 나이로 비교한다(링크가 나이를 싣는 것과 같은 이유).
  세션 번호·학습일은 이 기기 것을 유지하고, 시간 기록(timeLog)은 링크에 없어 합쳐지지 않는다.
  버튼에 합친 뒤 학습 수·늘어나는 수·단계 오르는 수를 미리 보여준다(`mergeSummary`)
- 리로드 없이 해시만 바뀌는 경우(이미 열어둔 창에 붙여넣기)도 `hashchange`로 받는다
- 적용/취소 후 `clearIncoming()`으로 주소의 코드를 지운다 (새로고침마다 다시 묻지 않게)

진도 표시는 **12루트 × 5quality 격자 히트맵 두 장(A형/B형)** 뿐. 칸 색 농도 = 단계.
네 장으로 쪼개지 말고 그래프도 만들지 마라.

## 설정 (`state/settings.ts`)

접힌 서랍 하나. **진행(메이저/마이너)과 폼(A/B)뿐** — 여기에 항목을 더 넣지 마라 (스펙 §7).
세션 크기는 설정이 아니라 시작 화면에 노출한다.

- 마지막 하나는 끌 수 없다(진행 둘 다 OFF, 폼 둘 다 OFF 금지) — 뽑을 게 없어지는 상태를 만들지 않는다
- 암기 `전체`의 풀만 좌우한다(`enabledItems`). 암기 `타입별`은 직접 지정이라 설정을 타지 않는다
- 출제 문맥도 제한한다(`createSession(..., allowedTypes)`): 마이너만 켜면 m7은 마이너 i로만 라벨된다.
  허용 문맥이 없는 quality(maj7 등)는 필터를 무시하고 원래 문맥을 쓴다 — 카드가 사라지면 안 된다

## 키보드 (전 과정 키보드만으로 조작 가능해야 함)

- 퀴즈: `Space` 정답 보기 · `1`/`2` 자가채점 · `Enter` 다음 · `Esc` 종료
- 채점 후 정정: `1` 맞음으로 넘김 · `2` 되돌리기.
  알고 있었는데 잘못 누른 경우를 오답으로 남기지 않는다. 카드에 반영되는 건 `next()` 시점이라
  `override()`는 표시값(`lastCorrect`)만 바꾸면 된다. 원래 채점값은 `autoCorrect`에 남겨
  "맞음으로 넘김"인지 "정답"인지 구분한다
- 종료 화면: `Enter` 한 판 더 · `Esc` 나가기
- 암기 시작 화면: `1~4` 세션 크기(12/30/60/120) · `Enter` 마지막 크기 / 타입별 시작
- 그 외 이동은 네이티브 Tab 포커스 — 그래서 `:focus-visible` 링을 지우지 마라

## 모바일

`Keyboard`가 스스로 페이징한다 (`useMediaQuery(NARROW)`, ≤640px). 스크롤 금지 — 한 옥타브(13반음)씩 `‹ ›`.
하이라이트가 **바뀔 때만** 창을 따라 옮긴다(ref 가드). 매 렌더마다 따라가면 사용자의 페이징이 즉시 되돌려진다.

## 하지 말 것

이론 설명/튜토리얼/온보딩(연결 힌트는 사실 한두 줄까지만), 계정/서버, 푸시 알림(서버가 필요하다),
악보 렌더링 라이브러리, 메트로놈, 청음 퀴즈, 게임화(배지/XP/스트릭 숫자 — 주간 원 표시까지만),
다크모드 토글(다크 하나만), SM-2, IndexedDB.
